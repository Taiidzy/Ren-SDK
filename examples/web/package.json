{
  "name": "ren-sdk-web-example",
  "version": "1.0.0",
  "description": "Ren SDK Web Example",
  "type": "module",
  "scripts": {
    "build": "cd ../.. && wasm-pack build --target web --out-dir examples/web/pkg --features wasm --no-default-features",
    "serve": "python -m http.server 8080"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.0"
  }
}

